requests==2.31.0
python-dotenv==1.0.1
psycopg[binary]==3.2.3
replicate==0.29.0
openai==1.53.0
httpx==0.27.0
requests-toolbelt==1.0.0
Pillow==10.4.0
rail# Arquivo: railway.yaml

version: 3

# Define a configuração do serviço principal (seu Python Worker)
services:
  app:
    name: Autopost Worker
    # O comando que o serviço executa, que neste caso é apenas um worker
    startCommand: "python src/main.py multirun --limit 1" 
    
    # Seção dedicada aos Jobs Agendados
    cron:
      # Job 1: 06:00 BRT (09:00 UTC)
      morning_post:
        schedule: "0 9 * * *"
        command: "python src/main.py autopost --disable_replicate --style \"street, candid, natural light\""

      # Job 2: 12:00 BRT (15:00 UTC)
      midday_post:
        schedule: "0 15 * * *"
        command: "python src/main.py autopost --disable_replicate --style \"portrait, studio-like natural light\""
        
      # Job 3: 19:00 BRT (22:00 UTC)
      evening_post:
        schedule: "0 22 * * *"
        command: "python src/main.py autopost --disable_replicate --style \"evening golden hour, city scenes\""way.yaml
